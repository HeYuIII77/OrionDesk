<Window x:Class="OrionDesk.UI.Windows.DocSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="文档中心设置"
        Width="460" Height="250"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FF1E1E21"
        Foreground="White"
        FontFamily="Segoe UI">
    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CCFFFFFF"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </Window.Resources>

    <Border Background="#FF1E1E21" Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题 -->
            <TextBlock Text="文档中心设置" FontSize="16" FontWeight="SemiBold"
                       Foreground="White" Margin="0,0,0,20"/>

            <!-- 文档目录 -->
            <TextBlock Text="文档目录" Style="{StaticResource LabelStyle}" Grid.Row="1"/>
            <Grid Grid.Row="2" Margin="0,0,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="PathBox" Style="{StaticResource InputStyle}"
                         VerticalContentAlignment="Center" Margin="0,0,8,0" Height="38"/>
                <Button Grid.Column="1" Content="浏览" Style="{StaticResource SecondaryButtonStyle}"
                        Click="BrowseButton_Click" Padding="14,6"/>
            </Grid>

            <!-- 间隔 -->
            <Border Grid.Row="3"/>

            <!-- 按钮 -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="确定" Style="{StaticResource ButtonStyle}"
                        Click="OkButton_Click" Margin="0,0,8,0" Padding="16,6"/>
                <Button Content="取消" Style="{StaticResource SecondaryButtonStyle}"
                        Click="CancelButton_Click" Padding="16,6"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
