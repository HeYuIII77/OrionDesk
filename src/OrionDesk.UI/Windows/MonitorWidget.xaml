<local:BaseWidgetWindow x:Class="OrionDesk.UI.Windows.MonitorWidget"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OrionDesk.UI.Windows"
        mc:Ignorable="d"
        Title="系统监控"
        Background="Transparent">
    <Window.Resources>
        <!-- 标题样式 -->
        <Style x:Key="TitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <!-- 数值样式 -->
        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CCFFFFFF"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- 进度条样式 -->
        <Style x:Key="MonitorProgressBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="6"/>
            <Setter Property="Background" Value="#1AFFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" CornerRadius="3"/>
                            <Border x:Name="PART_Track"/>
                            <Border x:Name="PART_Indicator" CornerRadius="3" HorizontalAlignment="Left">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                        <GradientStop Color="#FF30BB43" Offset="0"/>
                                        <GradientStop Color="#FF30BB43" Offset="0.6"/>
                                        <GradientStop Color="#FFFFB900" Offset="0.8"/>
                                        <GradientStop Color="#FFE74856" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Style="{StaticResource GlassBackground}">
        <!-- 右键菜单 -->
        <Border.ContextMenu>
            <ContextMenu>
                <MenuItem Header="刷新" Click="Refresh_Click"/>
                <Separator/>
                <MenuItem x:Name="LockMenuItem" Header="锁定" Click="Lock_Click"/>
                <Separator/>
                <MenuItem Header="关闭组件" Click="Close_Click"/>
            </ContextMenu>
        </Border.ContextMenu>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 右上角锁定按钮 -->
            <Button x:Name="LockButton" Content="🔓"
                    HorizontalAlignment="Left" VerticalAlignment="Center"
                    Style="{StaticResource LockButtonStyle}"
                    Click="Lock_Click" Margin="0,0,0,6"
                    ToolTip="锁定" Grid.Row="0"/>

            <!-- CPU 使用率 -->
            <StackPanel Grid.Row="1" Margin="0,0,0,12">
                <Grid>
                    <TextBlock Text="CPU" Style="{StaticResource TitleStyle}"/>
                    <TextBlock x:Name="CpuValue" Text="0%" Style="{StaticResource ValueStyle}" HorizontalAlignment="Right"/>
                </Grid>
                <ProgressBar x:Name="CpuBar" Style="{StaticResource MonitorProgressBar}" Value="0" Maximum="100" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- 内存使用率 -->
            <StackPanel Grid.Row="2" Margin="0,0,0,12">
                <Grid>
                    <TextBlock Text="内存" Style="{StaticResource TitleStyle}"/>
                    <TextBlock x:Name="MemoryValue" Text="0%" Style="{StaticResource ValueStyle}" HorizontalAlignment="Right"/>
                </Grid>
                <ProgressBar x:Name="MemoryBar" Style="{StaticResource MonitorProgressBar}" Value="0" Maximum="100" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- 磁盘使用率（动态添加） -->
            <StackPanel x:Name="DiskPanel" Grid.Row="3"/>

            <!-- 底部间距 -->
            <Grid Grid.Row="4"/>
        </Grid>
    </Border>
</local:BaseWidgetWindow>
