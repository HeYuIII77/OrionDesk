<local:BaseWidgetWindow x:Class="OrionDesk.UI.Windows.DocWidget"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:OrionDesk.UI.Windows"
        Title="文档中心"
        Background="Transparent">
    <Window.Resources>
        <!-- 覆盖系统高亮色 -->
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#FF2A2A2A"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#FF2A2A2A"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="White"/>

        <!-- TreeView 样式 -->
        <Style x:Key="DocTreeViewStyle" TargetType="TreeView">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="0,2"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled"/>
        </Style>

        <!-- TreeViewItem 样式 -->
        <Style x:Key="DocTreeViewItemStyle" TargetType="TreeViewItem">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Margin" Value="0,1"/>
            <Setter Property="IsExpanded" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <StackPanel>
                            <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                    CornerRadius="4" Padding="{TemplateBinding Padding}"
                                    Margin="{TemplateBinding Margin}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- 选中高亮条 -->
                                    <Border x:Name="SelectionBar" Grid.Column="0"
                                            Width="3" CornerRadius="1.5"
                                            Background="Transparent"
                                            Margin="0,2,4,2"/>

                                    <!-- 展开/折叠按钮 -->
                                    <ToggleButton x:Name="Expander" ClickMode="Press" Grid.Column="1"
                                                  IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                                  Visibility="{Binding HasItems, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToVis}}"
                                                  Background="Transparent" BorderThickness="0"
                                                  Width="16" Height="16" Margin="0,0,4,0">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <TextBlock x:Name="Arrow" Text="▶" FontSize="9"
                                                           Foreground="#88FFFFFF" VerticalAlignment="Center"
                                                           HorizontalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                                                    <TextBlock.RenderTransform>
                                                        <RotateTransform Angle="0"/>
                                                    </TextBlock.RenderTransform>
                                                </TextBlock>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="Arrow" Property="Text" Value="▼"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>

                                    <!-- 内容 -->
                                    <ContentPresenter x:Name="PART_Header" Grid.Column="2"
                                                      ContentSource="Header"
                                                      VerticalAlignment="Center"/>
                                </Grid>
                            </Border>

                            <!-- 子项（左缩进） -->
                            <ItemsPresenter x:Name="ItemsHost" Visibility="Collapsed" Margin="16,0,0,0"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="ItemsHost" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="SelectionBar" Property="Background" Value="{StaticResource AccentColor}"/>
                                <Setter TargetName="Bd" Property="Background" Value="#FF2A2A2A"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="IsSelectionActive" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="SelectionBar" Property="Background" Value="{StaticResource AccentColor}"/>
                                <Setter TargetName="Bd" Property="Background" Value="#FF2A2A2A"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsSelected" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd" Property="Background" Value="#10FFFFFF"/>
                            </MultiTrigger>
                            <!-- 拖放悬停高亮 -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd" Property="Background" Value="#22FFFFFF"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 搜索框样式 -->
        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#22FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#33FFFFFF"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>
    </Window.Resources>

    <Border Style="{StaticResource GlassBackground}">
        <!-- 右键菜单（空白区域） -->
        <Border.ContextMenu>
            <ContextMenu>
                <MenuItem Header="刷新" Click="Refresh_Click"/>
                <Separator/>
                <MenuItem Header="设置" Click="Settings_Click"/>
                <Separator/>
                <MenuItem x:Name="LockMenuItem" Header="锁定" Click="Lock_Click"/>
                <Separator/>
                <MenuItem Header="关闭组件" Click="Close_Click"/>
            </ContextMenu>
        </Border.ContextMenu>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Grid Grid.Row="0" Margin="0,0,0,4">
                <Button x:Name="LockButton" Content="🔓"
                        HorizontalAlignment="Left" VerticalAlignment="Center"
                        Style="{StaticResource LockButtonStyle}"
                        Click="Lock_Click" ToolTip="锁定"/>
                <TextBlock x:Name="TitleText" Text="文档中心"
                           Foreground="White" FontFamily="Segoe UI" FontSize="12"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Button x:Name="SettingsButton" Content="⚙"
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Style="{StaticResource LockButtonStyle}"
                        Click="Settings_Click" ToolTip="设置"/>
            </Grid>

            <!-- 搜索框 -->
            <Grid Grid.Row="1" Margin="0,0,0,4">
                <TextBox x:Name="SearchBox"
                         Style="{StaticResource SearchBoxStyle}"
                         TextChanged="SearchBox_TextChanged"
                         GotFocus="SearchBox_GotFocus"
                         LostFocus="SearchBox_LostFocus"/>
                <TextBlock x:Name="SearchPlaceholder" Text="🔍 搜索文档..."
                           Foreground="#55FFFFFF" FontSize="12" FontFamily="Segoe UI"
                           IsHitTestVisible="False"
                           VerticalAlignment="Center" Margin="9,0,0,0"/>
            </Grid>

            <!-- 提示文本（未设置目录时） -->
            <TextBlock x:Name="HintText" Text="⚙ 请先设置文档目录" Grid.Row="2"
                       Foreground="#55FFFFFF" FontSize="12" FontFamily="Segoe UI"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Visibility="Collapsed"/>

            <!-- 树形文件列表 -->
            <ScrollViewer Grid.Row="2" Style="{StaticResource DarkScrollViewerStyle}"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Auto">
                <TreeView x:Name="DocTree"
                          Style="{StaticResource DocTreeViewStyle}"
                          MouseDoubleClick="DocTree_MouseDoubleClick"
                          TreeViewItem.Expanded="DocTree_Expanded">
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem" BasedOn="{StaticResource DocTreeViewItemStyle}"/>
                    </TreeView.ItemContainerStyle>
                </TreeView>
            </ScrollViewer>
        </Grid>
    </Border>
</local:BaseWidgetWindow>
